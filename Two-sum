def twosum(arr,k):
    for i in range(len(arr)):
        for j in range(1,len(arr)):
          if(k==arr[i]+arr[j]):
             print(arr[i])
             print(arr[j])            
arr=[4,7,1,-3,2]
k=1
print(arr)
twosum(arr,k)
